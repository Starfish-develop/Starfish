language: python
python:
  - 3.2
  - 3.3
  - 3.4
#Because we are using the educational version, we can't do this
#before_install:
# - sudo apt-get update -qq
# - sudo apt-get install -qq build-essential
install:
  - pip install cython --user
  - python setup.py install
# command to run tests
#script: nosetests
