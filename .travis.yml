language: python
python:
  - 3.2
  - 3.3
  - 3.4
#Because we are using the educational version, we can't do this
#before_install:
# - sudo apt-get update -qq
# - sudo apt-get install -qq build-essential
# Need to install hdf5 headers here but we can't do that due to no sudo access on educational version
install:
  - pip install cython
  - pip install h5py
  - python setup.py install
# command to run tests
#script: nosetests
